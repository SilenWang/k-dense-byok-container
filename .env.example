# K-Dense BYOK container
#
# Copy this file to .env, fill in your keys, then launch:
#   docker compose up -d
# or (with pixi):
#   pixi run up
#
# GPU support (requires NVIDIA Container Toolkit):
#   docker compose -f docker-compose.yml -f docker-compose.gpu.yml up -d

# --- Model access (choose at least one; or connect a subscription in Settings) ---

# OpenRouter API key (broadest model access)
OPENROUTER_API_KEY=

# NVIDIA NIM API key (build.nvidia.com)
# NVIDIA_API_KEY=

# Modal remote compute (cloud CPU/GPU)
# MODAL_TOKEN_ID=
# MODAL_TOKEN_SECRET=

# --- Host Ollama ---
# Default points at the Docker HOST's Ollama via host.docker.internal.
# If using the bundled GPU sidecar, set:
#   OLLAMA_BASE_URL=http://ollama:11434
# and launch with:
#   docker compose -f docker-compose.yml -f docker-compose.gpu.yml --profile ollama up -d
OLLAMA_BASE_URL=http://host.docker.internal:11434

# --- Model defaults ---
DEFAULT_MODEL_ID=anthropic/claude-opus-5
DEFAULT_MODEL_PROVIDER=openrouter

# --- Ports (override if already in use) ---
# KADY_UI_PORT=3000
# KADY_API_PORT=8000

# --- Optional web-access keys ---
# EXA_API_KEY=
# PERPLEXITY_API_KEY=
# GEMINI_API_KEY=

# --- Proxy (optional) ---
# HTTPS_PROXY=http://proxy.example.com:3128
# HTTP_PROXY=http://proxy.example.com:3128
# NO_PROXY=localhost,127.0.0.1
